spring:
  kafka:
    kafka-server: localhost:9092

# Feign + CurcuitBreaker 활성화
feign:
  circuitbreaker:
    enabled: true

# Feign + CurcuitBreaker Transaction 처리
resilience4j:
  circuitbreaker:
    instances:
      productService:
        failure-rate-threshold: 40
        wait-duration-in-open-state: 10s